{
  "name": "classhub",
  "version": "1.0.0",
  "description": "Your classes, chapters & notes — all in one place",
  "main": "index.html",
  "scripts": {
    "dev": "npx http-server",
    "build": "echo 'Web app ready'",
    "cap:add:android": "npx cap add android",
    "cap:build": "npx cap build android",
    "cap:run": "npx cap run android",
    "cap:sync": "npx cap sync"
  },
  "keywords": ["education", "study", "notes", "classhub"],
  "author": "ClassHub",
  "license": "MIT",
  "dependencies": {
    "@capacitor/core": "^5.7.0",
    "@capacitor/android": "^5.7.0",
    "@supabase/supabase-js": "^2.38.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.7.0"
  }
}
